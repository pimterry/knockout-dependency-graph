"use strict";

define([
    'knockout3',
    'lib/knockout-dependency-graph'
], function(ko, koGrapher) {
    describe('Functional tests', function () {
        describe('Basic usage', function () {
           it('Should track dependencies between observables', function () {

           });
        });
    });
});